import { NextResponse } from 'next/server';
import { z } from 'zod';
import { getSession } from '@/lib/auth';
import { query } from '@/lib/db';

const commissionSchema = z.object({ rate: z.number().min(0).max(100), consultantId: z.string().uuid().optional() });

export async function POST(request: Request, { params }: { params: { id: string } }) { try { const session = await getSession(request); if (!session) return NextResponse.json({ error: 'Unauthenticated.' }, { status: 401 }); const body = commissionSchema.parse(await request.json()); const sale = await query('SELECT id, total, consultant_id FROM sales WHERE id = $1 AND organization_id = $2', [params.id, session.user.organizationId]); if (!sale.rows[0]) return NextResponse.json({ error: 'Sale not found.' }, { status: 404 }); const result = await query(`INSERT INTO commission_entries (organization_id, sale_id, consultant_id, rate, amount) VALUES ($1, $2, $3, $4, $5) ON CONFLICT (sale_id) DO UPDATE SET consultant_id = EXCLUDED.consultant_id, rate = EXCLUDED.rate, amount = EXCLUDED.amount, status = 'Provisional' RETURNING id, sale_id, consultant_id, rate, amount, status`, [session.user.organizationId, params.id, body.consultantId || sale.rows[0].consultant_id || null, body.rate, Number(sale.rows[0].total) * body.rate / 100]); return NextResponse.json({ commission: result.rows[0] }, { status: 201 }); } catch (error) { if (error instanceof z.ZodError) return NextResponse.json({ error: 'Commission rate is required.' }, { status: 400 }); console.error('Commission create failed', error); return NextResponse.json({ error: 'Unable to calculate commission.' }, { status: 500 }); } }
